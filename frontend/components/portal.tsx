"use client";
import { useState } from "react";
import { ChevronDown, ChevronRight, LockKeyhole, Radio, ShieldCheck, Terminal, UserRound, Zap, Database, GitBranch, Bot } from "lucide-react";
import { credentials } from "../lib/data";
import { login, setSession } from "../lib/auth";
import { useRouter } from "next/navigation";

export function Portal(){
 const router=useRouter();const [id,setId]=useState("");const [password,setPassword]=useState("");const [busy,setBusy]=useState(false);const [error,setError]=useState("");const [open,setOpen]=useState(false);const [log,setLog]=useState(["[SYS] DEVORA secure terminal online","[SYS] waiting for identity packet..."]);
 const submit=async(e:React.FormEvent)=>{e.preventDefault();setError("");setBusy(true);setLog(v=>[...v,"[AUTH] decrypting identity packet..."]);try{const s=await login(id,password);setSession(s);setLog(v=>[...v,"[AUTH] identity verified","[SYS] routing to command center..."]);router.push("/cockpit")}catch(err){setError((err as Error).message);setLog(v=>[...v,"[ERR] AUTH_FAILURE // packet rejected"])}finally{setBusy(false)}};
 const quick=(x:typeof credentials[number])=>{setId(x.id);setPassword(x.password);setError("")};
 return <main className="cyber-bg scanline flex min-h-screen items-center justify-center px-4 py-5 sm:px-6 lg:px-8"><div className="relative z-10 grid w-full max-w-7xl overflow-hidden rounded-[28px] border border-white/10 bg-[#060a17]/80 shadow-[0_30px_120px_rgba(0,0,0,.55)] backdrop-blur-2xl lg:grid-cols-[1.08fr_.92fr]">
  <section className="relative hidden min-h-[760px] overflow-hidden border-r border-white/10 p-10 lg:flex lg:flex-col lg:justify-between xl:p-14"><div className="absolute -left-24 top-16 h-72 w-72 rounded-full bg-purple-600/10 blur-3xl"/><div className="absolute -right-20 bottom-0 h-80 w-80 rounded-full bg-cyan-500/10 blur-3xl"/><div className="relative"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl border border-cyan-300/30 bg-cyan-300/10 text-cyan-300 shadow-[0_0_30px_rgba(34,211,238,.14)]"><Zap size={20}/></div><div><div className="text-xl font-black tracking-[.22em] text-white">DEVORA</div><div className="text-[9px] tracking-[.34em] text-cyan-300/60">DEVELOPER KNOWLEDGE SYSTEM</div></div></div><div className="mt-24 max-w-2xl"><div className="mb-5 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.32em] text-purple-300"><Radio size={13}/> AI-powered developer onboarding</div><h1 className="text-glow text-6xl font-black leading-[.94] tracking-[-.04em] text-white xl:text-7xl">Turn any<br/><span className="text-cyan-300">codebase</span><br/>into a learning journey.</h1><p className="mt-7 max-w-xl text-base leading-7 text-slate-400">DEVORA connects repository intelligence, project documentation, grounded answers and adaptive learning into one developer command center.</p></div></div><div className="relative grid grid-cols-2 gap-3 xl:grid-cols-4">{[[GitBranch,"REPOSITORY","ONLINE"],[Database,"KNOWLEDGE","ONLINE"],[ShieldCheck,"LEARNING","ONLINE"],[Bot,"BOB","ONLINE"]].map(([Icon,label,status])=><div key={label as string} className="glass rounded-xl p-4"><Icon size={15} className="text-cyan-300"/><div className="mt-3 text-[8px] font-bold tracking-[.18em] text-slate-500">{label as string}</div><div className="mt-1 flex items-center gap-1 text-[8px] text-emerald-300"><span className="h-1.5 w-1.5 rounded-full bg-emerald-300"/>{status as string}</div></div>)}</div></section>
  <section className="flex min-h-[760px] flex-col justify-center p-6 sm:p-10 lg:p-12 xl:p-16"><div className="mb-8 lg:hidden"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl border border-cyan-300/30 bg-cyan-300/10 text-cyan-300"><Zap size={18}/></div><div className="text-xl font-black tracking-[.2em] text-white">DEVORA</div></div></div><div className="mb-8 flex items-start justify-between gap-4"><div><div className="mb-2 flex items-center gap-2 text-[9px] font-bold uppercase tracking-[.3em] text-cyan-300"><ShieldCheck size={13}/> Secure Access Portal</div><h2 className="text-3xl font-black text-white sm:text-4xl">Initialize your session.</h2><p className="mt-2 text-sm text-slate-500">Authenticate to enter the developer cockpit.</p></div><span className="rounded-full border border-emerald-300/20 bg-emerald-300/5 px-3 py-1.5 text-[8px] font-black tracking-widest text-emerald-300">SECURE</span></div>{error&&<div className="mb-5 flex gap-3 rounded-xl border border-red-400/20 bg-red-400/5 p-4 font-mono text-[10px] leading-5 text-red-300"><Terminal size={14} className="mt-0.5 shrink-0"/>{error}</div>}<form onSubmit={submit} className="space-y-5"><Field label="Username / ID" icon={<UserRound size={14}/>} value={id} onChange={setId} placeholder="e.g. ankita"/><Field label="Access Key" icon={<LockKeyhole size={14}/>} value={password} onChange={setPassword} placeholder="••••••••••" type="password"/><button disabled={busy||!id||!password} className="group relative flex h-14 w-full items-center justify-center gap-3 overflow-hidden rounded-xl border border-purple-300/35 bg-gradient-to-r from-purple-600 to-cyan-500 font-black tracking-[.17em] text-white shadow-[0_0_35px_rgba(139,92,246,.2)] transition hover:shadow-[0_0_50px_rgba(34,211,238,.2)] disabled:cursor-not-allowed disabled:opacity-40">{busy?<><Terminal size={16} className="animate-decrypt"/> DECRYPTING IDENTITY...</>:<>INITIALIZE SESSION <ChevronRight size={17}/></>}</button></form><div className="mt-7 overflow-hidden rounded-2xl border border-white/8 bg-black/20"><button onClick={()=>setOpen(v=>!v)} className="flex w-full items-center justify-between p-4 text-left"><span><span className="block text-[10px] font-black uppercase tracking-[.2em] text-slate-300">Credentials Reference Sheet</span><span className="mt-1 block text-[10px] text-slate-600">Quick-fill demo identities for local testing</span></span>{open?<ChevronDown size={16}/>:<ChevronRight size={16}/>}</button>{open&&<div className="border-t border-white/8 p-3">{credentials.map(c=><button key={c.id} onClick={()=>quick(c)} className="mb-2 flex w-full items-center justify-between rounded-xl border border-white/5 bg-white/[.02] p-3 text-left transition last:mb-0 hover:border-cyan-300/25 hover:bg-cyan-300/[.04]"><span><span className="block text-xs font-bold text-white">{c.name}</span><span className="font-mono text-[9px] text-cyan-300/70">{c.id}</span></span><span className="text-[8px] uppercase tracking-widest text-slate-600">{c.role}</span></button>)}</div>}</div><div className="mt-6 flex items-center gap-2 font-mono text-[9px] text-slate-700"><Terminal size={11}/><span className="truncate">{log[log.length-1]}</span></div></section>
 </div></main>
}
function Field({label,icon,value,onChange,placeholder,type="text"}:{label:string;icon:React.ReactNode;value:string;onChange:(v:string)=>void;placeholder:string;type?:string}){return <label className="block"><span className="mb-2 flex items-center gap-2 text-[9px] font-bold uppercase tracking-[.2em] text-slate-500">{icon}{label}</span><input required type={type} value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder} className="h-14 w-full rounded-xl border border-white/10 bg-[#090f22]/80 px-4 font-mono text-sm text-white outline-none transition placeholder:text-slate-700 focus:border-cyan-300/45 focus:bg-cyan-300/[.02] focus:shadow-[0_0_28px_rgba(34,211,238,.1)]"/></label>}
