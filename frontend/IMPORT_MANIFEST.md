# Devora Frontend Import Manifest

This project was imported from `devora-frontend.zip`.

The uploaded application source and configuration were overlaid without deleting or rewriting existing source files. The manifest is the only added file created by the import workflow.

Verification performed:

- TypeScript check: `pnpm check`
- Production build: `pnpm build`

Both commands completed successfully.

The application source remains under `client/`, server code remains under `server/`, and shared code remains under `shared/`.

This file is not part of the application runtime.

